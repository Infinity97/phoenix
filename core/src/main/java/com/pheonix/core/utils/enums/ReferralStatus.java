package com.pheonix.core.utils.enums;

public enum ReferralStatus {

	LIVE,//If a referral programme is live on the system
	EXPIRED,// If a referral programme has expired
	CANCELLED,// If a referral programme is cancelled by the user
	DRAFT,// If it is Drafted but is not live
	SCHEDULED,// If a referral programme is accepted and is scheduled to be started but has not started yet.

}
